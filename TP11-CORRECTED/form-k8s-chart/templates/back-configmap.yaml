apiVersion: v1
kind: ConfigMap
metadata:
  name: back-configmap
  namespace: form-k8s-{{ .Values.env }}
data:
  MONGO_HOSTNAME: mongo.form-k8s-{{ .Values.env }}.svc.cluster.local
  MONGO_PORT: "27017"
  MONGO_DB: training